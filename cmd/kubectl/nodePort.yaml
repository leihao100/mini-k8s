apiVersion: v1
kind: Service
metadata:
  name: example-nodePort
  labels:
    app: example
spec:
  type: NodePort
  ports:
    - name: hello
      port: 30001
      targetPort: 5678
  selector:
      app: example

